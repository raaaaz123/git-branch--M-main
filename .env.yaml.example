# Environment variables for GCP Cloud Run deployment
# Copy this file to .env.yaml and fill in your actual values
# Then deploy with: gcloud run deploy --env-vars-file .env.yaml

PINECONE_API_KEY: "pcsk_your_pinecone_api_key"
PINECONE_INDEX_NAME: "rexa-engage"
OPENAI_API_KEY: "sk-your_openai_api_key"
OPENROUTER_API_KEY: "sk-or-v1-your_openrouter_api_key"
OPENROUTER_SITE_URL: "https://your-production-site.com"
OPENROUTER_SITE_NAME: "Rexa Engage"
FIREBASE_PROJECT_ID: "rexa-engage"
FIREBASE_PRIVATE_KEY_ID: "your_private_key_id"
FIREBASE_PRIVATE_KEY: "-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----"
FIREBASE_CLIENT_EMAIL: "firebase-adminsdk@rexa-engage.iam.gserviceaccount.com"
FIREBASE_CLIENT_ID: "your_client_id"
ALLOWED_ORIGINS: "https://your-frontend.com,https://your-app.vercel.app"

# IMPORTANT NOTES:
# 1. Never commit this file to Git! Add .env.yaml to .gitignore
# 2. For production, use GCP Secret Manager instead
# 3. Firebase private key should include \n for newlines

